# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_etl
DB_USER=postgres
DB_PASSWORD=your_password_here

# API Configuration (CoinGecko is free, no key needed)
COINGECKO_API_URL=https://api.coingecko.com/api/v3

# ETL Configuration
ETL_BATCH_SIZE=100
ETL_REFRESH_INTERVAL=300  # seconds

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/etl_pipeline.log